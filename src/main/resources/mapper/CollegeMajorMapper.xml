<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.graduation.freshmanwelcome.mapper.CollegeMajorMapper">

    <select id="queryBuId" resultType="org.graduation.freshmanwelcome.entity.CollegeMajor">
        select * from college_major where college_major_id=#{collegeMajorId}
    </select>
</mapper>
